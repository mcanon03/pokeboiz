
- add linting: prettier
- fix routing
  - filter, got back to home --> filter is not reset
- add ability to select multiple types
- filter types by generation (?)
  - if nothing present, add text
  - add default image
- get pokemon by generation
- add PWA